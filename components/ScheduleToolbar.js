export default {
    props: {
        
    },
    setup() {

        // This component is the top toolbar above the schedule.
        // It is mainly presentational (HTML + CSS), no complex logic needed.
        // so most of the work will be done here under 'template:' and in style.css.

        return {

        }

    },

    template: `
        <div class="menu-header"></div>
    `
}